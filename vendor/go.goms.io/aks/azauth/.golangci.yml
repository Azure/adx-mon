linters:
  enable-all: true
  disable:
    - gochecknoglobals
    - gochecknoinits
    - interfacer
    - testpackage

linters-settings:
  funlen:
    lines: 80
    statements: 60

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gomnd # disable magic number check in tests